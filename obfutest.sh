#!/bin/bash
# This is a basic obfuscation test in bash
# This method was first implemented in Batch on Windows XP by
# a writer in the BATCH ZONE zine.

o="a"
w="c"
q="h"
r="g"
x="e"
d="s"
t="o"
k="w"

$x$w$q$t $d$k$o$r

# The same method doesn't work in bash unfortunately, but a variant of it is possible as seen above.
# I'm going to do more testing and see if I can fully implement variable-based obfuscation
